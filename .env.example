# =============================================================================
# Variables de Entorno - App Bodegas
# =============================================================================
# Copia este archivo a .env y configura los valores

# Base de datos PostgreSQL (Neon u otro proveedor)
DATABASE_URL="postgresql://usuario:contraseña@host/basedatos?sslmode=require"

# NextAuth.js - Configuración de autenticación
# IMPORTANTE: Genera un secret único con: openssl rand -base64 32
NEXTAUTH_SECRET="genera-un-secret-unico-aqui"
NEXTAUTH_URL="http://localhost:3000"
